// EditionNouvellesFormes
// 2015
// NodeModel Backbone Model
//
define(['underscore', 'jquery', 'backbone', 'stache!square'], function (_, $, Backbone, template) {
	return Backbone.Model.extend({
		initialize:function(){
			console.log('new Node',this);
		},
		render:function(){

		}
	});
});